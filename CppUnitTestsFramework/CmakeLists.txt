# Create a library called "Hello" which includes the source file "hello.cxx".
# The extension is already found. Any number of sources could be listed here.
add_library (CppUnitTestsFramework FunctionTester.cpp HelperFuncs.cpp TestCase.cpp TestCategory.cpp TestContainer.cpp TestRegisterer.cpp TestReport.cpp TestSuite.cpp TestSuiteCreator.cpp ToStringConverter.cpp)

# Make sure the compiler can find include files for our Hello library
# when other libraries or executables link to Hello
target_include_directories (CppUnitTestsFramework PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})